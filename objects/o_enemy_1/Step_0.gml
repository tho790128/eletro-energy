


if atacado
{
    estado = explode_estado
}

move_and_collide(velh,velv,global.colisores)
if instance_exists(o_player)
{
    estado()
    
    //if point_distance(x,y,o_player.x,o_player.y) <= 30 && o_player.estado == o_player.dash_estado
    //{
        //dir = point_direction(x,y,o_player.x,o_player.y)
        //estado = knockback_estado
    //}
}
